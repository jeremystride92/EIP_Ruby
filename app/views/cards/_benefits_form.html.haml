= simple_form_for [ :venue, card ] do |f|
  = f.simple_fields_for :benefits do |ff|
    = render 'benefit_fields', f: ff
  .links
    = link_to_add_association 'Add Benefit', f, :benefits, :render_options => {:wrapper => 'bootstrap' }
  = f.button :submit
