%h3 Are you sure that you want to delete this user?

= simple_form_for [:admin, @user], method: 'delete' do |f|
  = f.button :submit, value: 'Delete', class: 'btn btn-danger'
  = link_to 'Cancel', admin_users_path, class: 'btn'
